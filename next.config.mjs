const nextConfig = {
  basePath: "/cards-nextjs-proj", 
  output: "export",  
  trailingSlash: true,  
  reactStrictMode: true,  
  images: {
    unoptimized: true,
  },
};

export default nextConfig;